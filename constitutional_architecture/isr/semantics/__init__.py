"""ISR semantic projection package (Phase-28 identity migration).

Single source of truth for the semantic architectural identity: see
``projection.py``. The ISR model's ``content_hash`` and tiannara's
``identity.FSMSemanticProjector`` both delegate here.
"""
