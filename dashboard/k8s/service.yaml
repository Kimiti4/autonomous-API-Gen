apiVersion: v1
kind: Service
metadata:
  name: esap-dashboard
  labels: { app: esap-dashboard }
spec:
  selector: { app: esap-dashboard }
  ports:
    - name: http
      port: 80
      targetPort: 8080
