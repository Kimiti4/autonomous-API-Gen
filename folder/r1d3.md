Below is the **full, execution-ready R1-D.3 Master Prompt**, designed to continue directly from the accepted R1-D.2 state while preserving the same evidence-first, contract-first, phase-gated discipline.

# TIA-R1-D.3 — EVOLUTION / EIR SEMANTIC MIGRATION

## AUTHORITATIVE MASTER PROMPT

**Repository:** `Kimiti4/autonomous-API-Gen`
**Branch:** `main`
**Current accepted baseline:** `4ae1b10`
**Previous milestone:** R1-D.2 — Canonical Compiler IR Semantic Migration — **PASS**

---

# 0. MANDATE

You are operating as the **Tiannara Architecture Evolution Auditor and Migration Engineer**.

Your responsibility in R1-D.3 is to determine, with repository evidence, whether the current Evolution/EIR substrate can be consolidated into **one canonical evolution model** without weakening:

* architectural integrity
* semantic fidelity
* provenance
* lineage
* determinism
* reproducibility
* verification
* certification
* evolutionary correctness
* backend independence
* historical evidence

This is a **semantic migration and consolidation phase**.

It is **NOT**:

* a broad refactor
* a feature-development phase
* a portfolio-generation phase
* a performance-optimization phase
* a rewrite of the Evolution Engine
* an implementation-technology migration
* a category-specific compiler project
* an opportunity to redesign unrelated subsystems

The objective is to establish the canonical evolutionary substrate that will eventually allow Tiannara to evolve software architectures autonomously.

---

# 1. CONSTITUTIONAL OBJECTIVE

Tiannara must ultimately implement the following conceptual pipeline:

```text
Requirements
      ↓
Requirement Graph
      ↓
Canonical ISR
      ↓
Architecture Model
      ↓
Canonical Compiler IR
      ↓
Compiler Backend
      ↓
ArtifactSet
      ↓
Verification
      ↓
Runtime Evidence
      ↓
Evolution Engine
      ↓
Candidate Architectures
      ↓
Evaluation
      ↓
Selection
      ↓
New Architecture
      ↓
Compilation
      ↓
Verification
      ↓
Runtime Evidence
      ↓
Evolution Feedback
```

R1-D.3 establishes the **Evolution Engine side of this loop**.

The Evolution Engine must operate on architectural representations and their evidence.

It must **not become a second compiler**.

It must **not become a second ISR**.

It must **not encode implementation technologies as evolutionary semantics**.

---

# 2. NON-NEGOTIABLE ARCHITECTURAL PRINCIPLE

The system must have exactly one authoritative semantic model for each constitutional layer.

Therefore R1-D.3 must establish:

```text
RequirementGraph      → one canonical authority
ISR                   → one canonical authority
ArchitectureModel     → one canonical authority
CompilerIR            → one canonical authority
EvolutionOperation    → one canonical authority
EvolutionRecord/EIR   → one canonical authority
VerificationResult    → one canonical authority
CertificationEvidence → one canonical authority
RuntimeObservation    → one canonical authority
```

Existing duplicate implementations may remain temporarily as:

* semantic donors
* compatibility surfaces
* legacy adapters
* historical evidence

but they must not remain competing authorities.

---

# 3. CANONICAL EVOLUTION POSITION

The target architecture is:

```text
                ┌────────────────────┐
                │  Architecture      │
                │  Candidate         │
                └─────────┬──────────┘
                          │
                    EvolutionOperation
                          │
                          ▼
                ┌────────────────────┐
                │ Evolution Engine   │
                │                    │
                │ mutation           │
                │ crossover          │
                │ transformation     │
                │ evaluation         │
                │ selection          │
                │ constraints        │
                └─────────┬──────────┘
                          │
                    EvolutionRecord
                          │
                          ▼
                ┌────────────────────┐
                │ New Architecture   │
                │ Candidate          │
                └────────────────────┘
```

The Evolution Record must establish the lineage:

```text
parent architecture
        ↓
evolution operation
        ↓
operator parameters
        ↓
constraints
        ↓
evaluation
        ↓
selection decision
        ↓
child architecture
```

No evolution event should become an unverifiable mutation of opaque state.

---

# 4. SCOPE

R1-D.3 is limited to:

### D3-01

Evolution substrate inventory.

### D3-02

Evolution execution graph.

### D3-03

Semantic comparison of evolution models.

### D3-04

Canonical EvolutionOperation contract.

### D3-05

Canonical EvolutionRecord/EIR contract.

### D3-06

Architecture Candidate relationship analysis.

### D3-07

Evolution lineage/provenance relationship analysis.

### D3-08

Migration map.

### D3-09

Consumer migration/reconciliation.

### D3-10

Legacy disposition.

### D3-11

Canonical integration analysis.

### D3-12

Contract and regression tests.

### D3-13

Final R1-D.3 gate.

Nothing beyond this scope may be implemented without separate authorization.

---

# 5. OUT OF SCOPE

Do NOT perform:

* portfolio application generation
* new application backends
* deployment work
* UI development
* observatory redesign
* new evolutionary algorithms
* reinforcement-learning implementation
* genetic-algorithm optimization
* population scaling
* distributed evolution redesign
* GPU evolution optimization
* autonomous self-modification
* production evolution
* new fitness functions
* new benchmark campaigns
* B3-v2 reruns
* certification campaign reruns
* historical ledger rewriting
* broad dependency upgrades
* unrelated test repairs
* cleanup unrelated to D.3
* deletion of legacy systems without disposition evidence

If such work is discovered as necessary, classify it as:

```text
DEFERRED
```

with rationale.

---

# 6. BASELINE INTEGRITY

Before any modification:

1. Verify current branch.
2. Verify current HEAD.
3. Verify working tree.
4. Verify `origin/main`.
5. Confirm baseline commit is:

```text
4ae1b10
```

or explicitly report the actual repository state if it differs.

6. Verify prior R1-D.1 and R1-D.2 artifacts exist.
7. Verify historical B3-v2 evidence remains untouched.
8. Verify existing certification ledgers remain untouched.

If the baseline is not clean or differs materially from the accepted state:

**STOP.**

Do not silently continue from an unknown baseline.

---

# 7. EVIDENCE-FIRST RULE

Do not infer architecture from filenames.

Do not infer authority from directory names.

Do not infer canonical status from class names.

Do not infer runtime usage from documentation.

For every important conclusion establish:

```text
FILE
SYMBOL
CALLER
CALL PATH
DATA FLOW
TEST EVIDENCE
RUNTIME EVIDENCE
```

Where runtime evidence does not exist, explicitly state:

```text
NO RUNTIME EVIDENCE
```

Do not upgrade static evidence into runtime claims.

---

# 8. PRIMARY QUESTIONS

R1-D.3 must answer these questions.

## Q1 — What is the real Evolution Engine?

Identify all implementations of:

* evolution
* mutation
* crossover
* transformation
* selection
* evaluation
* candidate generation
* population management
* lineage
* fitness
* constraints
* evolutionary policy
* architecture transformation

Determine which are:

```text
CANONICAL
SEMANTIC DONOR
ACTIVE LEGACY
DORMANT
EXPERIMENTAL
DEAD
DUPLICATE
```

---

## Q2 — What is the canonical EvolutionOperation?

Determine the semantic fields required to describe an evolutionary operation.

At minimum investigate:

```text
operation identity
operation type
parent architecture
target architecture
operator
parameters
constraints
preconditions
postconditions
randomness
seed
environment
actor/origin
timestamp
provenance
verification relationship
```

Do not automatically require every field.

Each field must have evidence-based justification.

---

## Q3 — What is the canonical EvolutionRecord/EIR?

Determine whether an Evolution Record is:

1. an operation
2. an event
3. a lineage record
4. a mutation description
5. an evaluation result
6. a composite record

Do not conflate these concepts.

The final contract must distinguish:

```text
WHAT changed
WHY it changed
HOW it changed
FROM what
TO what
UNDER what constraints
WITH what evidence
WITH what result
```

---

# 9. CRITICAL SEMANTIC DISTINCTION

Do not collapse:

```text
EvolutionOperation
```

and

```text
EvolutionRecord
```

unless repository evidence proves they are semantically identical.

Preferred conceptual model:

```text
EvolutionOperation
        │
        │ executes
        ▼
Architecture transition
        │
        ▼
EvolutionRecord
        │
        ├── parent
        ├── operation
        ├── child
        ├── evaluation
        ├── selection
        ├── provenance
        └── evidence
```

An operation describes an attempted transformation.

A record describes the resulting evolutionary event and lineage.

If the repository proves a different model is superior, document the evidence and revise accordingly.

---

# 10. ARCHITECTURE CANDIDATE

R1-D.3 must explicitly determine the relationship between:

```text
ArchitectureCandidate
```

and

```text
EvolutionRecord
```

The following distinction should be tested:

```text
ArchitectureCandidate
    = state / object being evolved

EvolutionOperation
    = transformation applied to candidate

EvolutionRecord
    = immutable historical account of transition

Evaluation
    = evidence about candidate quality

Selection
    = decision over competing candidates
```

Do not merge these merely for implementation convenience.

---

# 11. EVOLUTIONARY LINEAGE

Lineage must be explicit.

At minimum the conceptual graph must support:

```text
Candidate A
   │
   ├── operation O1
   │
   ▼
Candidate B
   │
   ├── operation O2
   │
   ▼
Candidate C
```

And branching:

```text
             Candidate A
              /       \
             /         \
           O1           O2
           ↓             ↓
       Candidate B   Candidate C
```

And crossover:

```text
Candidate A ─────┐
                  ├── Crossover ──→ Candidate C
Candidate B ─────┘
```

Lineage must remain reconstructable.

---

# 12. MUTATION SEMANTICS

Inventory every mutation mechanism.

For each mutation determine:

```text
input
operation
constraints
output
determinism
randomness
seed
semantic validity
lineage
verification
```

A mutation must not silently modify:

* implementation artifacts
* generated files
* backend-specific configuration

unless that behavior is explicitly part of the architecture model and contract.

Preferred evolutionary target:

```text
Architecture
```

rather than:

```text
Generated source code
```

---

# 13. CROSSOVER SEMANTICS

The existing root Evolution Engine contains evidence of real crossover.

Do not assume that this implementation is automatically canonical.

Determine:

* what is crossed
* whether crossover operates on architecture or code
* parent identity
* child identity
* deterministic behavior
* seed handling
* constraint preservation
* semantic validity
* lineage preservation
* evaluation
* failure behavior

If crossover is currently implemented correctly but embedded in a non-canonical structure, migrate its semantics rather than blindly copying its implementation.

---

# 14. EVALUATION SEMANTICS

Determine what constitutes evolutionary fitness.

Separate:

```text
evaluation
```

from:

```text
selection
```

Evaluation answers:

```text
How well does this candidate satisfy the evaluation criteria?
```

Selection answers:

```text
Which candidate survives or advances?
```

Do not make the EvolutionOperation responsible for either unless evidence supports that design.

Evaluation may consume:

```text
VerificationResult
RuntimeObservation
CertificationEvidence
performance evidence
quality evidence
security evidence
cost evidence
reliability evidence
```

R1-D.3 must establish the interface relationship without inventing new evaluation systems.

---

# 15. FAILURE SEMANTICS

Evolution must be fail-closed where semantic correctness is required.

Investigate:

```text
invalid mutation
invalid crossover
constraint violation
verification failure
evaluation failure
selection failure
lineage failure
serialization failure
provenance failure
duplicate candidate
non-deterministic operation
missing parent
missing evidence
```

For each classify:

```text
REJECT
FAIL
INDETERMINATE
DEFER
WARN
```

Warnings must not masquerade as successful evolutionary transitions.

---

# 16. DETERMINISM

Determine whether evolutionary operations are:

```text
deterministic
seeded deterministic
intentionally stochastic
non-deterministic
unknown
```

If randomness exists, establish the semantic requirement for:

```text
seed
random source identity
operator identity
parameterization
environment
```

The objective is reproducibility.

Do not remove legitimate stochastic evolution merely to make implementation deterministic.

Instead make stochasticity explicit and replayable where possible.

---

# 17. PROVENANCE

Every canonical evolutionary transition must support provenance sufficient to answer:

```text
Which architecture produced this candidate?
Which operation produced it?
Which parameters were used?
Which engine/version executed it?
Which constraints applied?
Which evaluation produced the score?
Which verification evidence existed?
Which runtime evidence contributed?
```

Do not introduce a second provenance store.

Use existing canonical provenance/ledger infrastructure where appropriate.

---

# 18. HISTORICAL IMMUTABILITY

Existing evolutionary evidence is historical evidence.

Therefore:

```text
DO NOT rewrite historical records.
DO NOT recompute historical hashes and replace them.
DO NOT alter old campaign results.
DO NOT normalize historical schemas in-place.
DO NOT make old records appear to have been generated by the new system.
```

If compatibility is required:

```text
LEGACY → CANONICAL
```

with explicit provenance.

Never:

```text
CANONICAL ↔ LEGACY
```

as a bidirectional semantic authority.

---

# 19. SEMANTIC DONOR RULE

Potential donors include:

```text
root evolution/
constitutional_architecture/evolution/
EIR models
mutation implementations
crossover implementations
selection implementations
lineage implementations
fitness/evaluation models
distributed evolution/
civilization/
```

Do not automatically designate any as canonical.

For each donor classify:

```text
KEEP
MIGRATE
ADAPT
DEPRECATE
RETIRE
DEFER
```

Semantic value and implementation authority are separate decisions.

---

# 20. DISTRIBUTED EVOLUTION BOUNDARY

Investigate `distributed_evolution/`.

Determine whether it contains:

```text
evolution semantics
```

or merely:

```text
execution infrastructure
```

If it is infrastructure, it must not become the canonical Evolution Engine.

Preferred separation:

```text
Evolution Engine
        ↓
Evolution Operations
        ↓
Execution Strategy
        ↓
local/distributed backend
```

Technology-specific distribution mechanisms remain implementation infrastructure.

---

# 21. CATEGORY-SPECIFIC EVOLUTION

Search for category-specific evolution systems.

Examples include evolutionary logic embedded in:

```text
category compilers
backend modules
domain generators
application generators
specialized optimization modules
```

No category may become an independent evolutionary authority.

Category-specific strategies may exist as:

```text
plugins
operators
constraints
fitness functions
backend capabilities
```

but canonical evolutionary semantics remain centralized.

---

# 22. REQUIRED DELIVERABLES

Create the following artifacts.

## D3-01

`folder/R1_D3_EVOLUTION_INVENTORY.md`

Must contain:

* all evolution implementations
* symbols
* consumers
* runtime status
* tests
* authority classification
* semantic ownership
* duplicate identification

---

## D3-02

`folder/R1_D3_EVOLUTION_EXECUTION_GRAPH.md`

Must document:

```text
entrypoint
→ candidate
→ operation
→ mutation/crossover
→ constraints
→ evaluation
→ selection
→ lineage
→ provenance
→ verification
```

Mark every edge as:

```text
STATIC
TESTED
RUNTIME
UNKNOWN
```

---

## D3-03

`folder/R1_D3_EVOLUTION_SEMANTIC_COMPARISON.md`

Compare:

* root evolution model
* constitutional EIR model
* other discovered evolution models

Compare fields and semantics for:

```text
candidate
operation
mutation
crossover
evaluation
selection
lineage
constraints
provenance
determinism
failure
verification
```

---

## D3-04

Update:

`folder/CONTRACT_EvolutionOperation.md`

The contract must define:

* purpose
* semantic identity
* required fields
* optional fields
* invariants
* preconditions
* postconditions
* determinism
* randomness
* failure semantics
* provenance
* compatibility

---

## D3-05

Update:

`folder/CONTRACT_EvolutionRecord_EIR.md`

The contract must define:

* event identity
* parent identity
* operation identity
* child identity
* transition semantics
* evaluation relationship
* selection relationship
* verification relationship
* provenance
* lineage
* immutability
* failure semantics

---

## D3-06

Update or establish:

`folder/CONTRACT_ArchitectureCandidate.md`

Explicitly define:

```text
candidate identity
architecture identity
version
state
parent relationship
lineage
evaluation
constraints
provenance
```

Do not duplicate ISR semantics.

---

## D3-07

Create:

`folder/R1_D3_EVOLUTION_LINEAGE_MODEL.md`

Define the canonical lineage graph.

---

## D3-08

Create:

`folder/R1_D3_EVOLUTION_MIGRATION_MAP.md`

Every discovered implementation must receive a disposition:

```text
MIGRATE
ADAPT
KEEP
DEPRECATE
RETIRE
DEFER
```

No unexplained duplicate authority may remain.

---

## D3-09

Create:

`folder/R1_D3_EVOLUTION_CONSUMER_MIGRATION.md`

Inventory every consumer and classify:

```text
CANONICAL
MIGRATED
LEGACY
ADAPTER
UNUSED
DEFERRED
```

---

## D3-10

Create:

`folder/R1_D3_EVOLUTION_LEGACY_DISPOSITION.md`

Document:

* constitutional EIR
* legacy evolution APIs
* distributed evolution implementations
* category-specific evolution
* obsolete evolution models
* compatibility adapters

---

## D3-11

Create:

`folder/R1_D3_CANONICAL_EVOLUTION_INTEGRATION_REPORT.md`

Prove:

```text
RequirementGraph
      ↓
ISR
      ↓
ArchitectureModel
      ↓
CompilerIR
      ↓
ArtifactSet
      ↓
Verification
      ↓
RuntimeEvidence
      ↓
Evolution
```

and establish the reverse feedback path:

```text
RuntimeEvidence
      ↓
Evaluation
      ↓
Selection
      ↓
Evolution
      ↓
New Architecture
```

---

## D3-12

Create:

`folder/R1_D3_EVOLUTION_TEST_REPORT.md`

Include:

* contract tests
* lineage tests
* operation tests
* mutation tests
* crossover tests
* determinism tests
* failure tests
* provenance tests
* compatibility tests
* regression results

---

## D3-13

Create:

`folder/R1_D3_GATE_REPORT.md`

This is the authoritative phase verdict.

---

# 23. TEST REQUIREMENTS

Tests must prove semantics, not merely object construction.

At minimum test:

### Contract

* valid operation
* invalid operation
* required identity
* invalid state

### Lineage

* parent → child
* multi-generation lineage
* branching
* crossover
* missing parent rejection

### Mutation

* valid mutation
* invalid mutation
* constraint violation

### Crossover

* two parents
* child lineage
* reproducibility
* invalid crossover

### Determinism

* same seed → reproducible result
* different seed → permissible variation
* stochasticity explicitly recorded

### Provenance

* operation provenance
* candidate provenance
* evaluation provenance
* verification linkage

### Failure

* mutation failure
* evaluation failure
* verification failure
* provenance failure

### Compatibility

* legacy → canonical
* no canonical → legacy semantic mutation
* historical records remain immutable

---

# 24. TEST DISCIPLINE

Do not weaken existing tests to achieve green.

Do not:

* delete tests
* reduce assertions
* skip failing cases
* convert failures to warnings
* change historical expectations without justification
* broaden tolerances merely to pass

If an existing test encodes obsolete semantics:

1. identify it
2. explain why
3. update it only if the canonical contract requires the change
4. preserve historical evidence

---

# 25. CODE CHANGE POLICY

R1-D.3 permits production-code changes only when required to establish the canonical Evolution/EIR boundary.

Allowed:

* canonical contract implementation
* narrow adapter implementation
* consumer import/API reconciliation
* semantic-preservation fixes
* contract tests
* lineage/provenance boundary fixes

Forbidden:

* unrelated refactors
* style-only changes
* performance rewrites
* broad architecture rewrites
* new evolution algorithms
* new distributed execution infrastructure

Prefer the smallest semantic change that establishes canonical ownership.

---

# 26. NO SILENT SEMANTIC LOSS

If migrating:

```text
old field → new field
```

prove semantic equivalence.

If there is no equivalent:

```text
DEFER
```

or explicitly introduce a new canonical field with rationale.

Never silently discard:

* parent identity
* operation identity
* lineage
* mutation parameters
* random seed
* constraints
* evaluation evidence
* verification evidence
* provenance

---

# 27. ADAPTER RULE

All legacy adapters must be one-way:

```text
LEGACY
   ↓
CANONICAL
```

They must not:

* become new authorities
* redefine canonical semantics
* perform bidirectional synchronization
* mutate historical evidence
* silently coerce incompatible semantics

Every adapter must have:

* explicit source model
* explicit target model
* semantic mapping
* unsupported-field behavior
* provenance preservation
* tests

---

# 28. EVOLUTION ↔ VERIFICATION

Evolution must not bypass verification.

Preferred flow:

```text
Candidate
   ↓
Compile
   ↓
ArtifactSet
   ↓
Verify
   ↓
Evidence
   ↓
Evaluate
   ↓
Select
```

If the repository currently permits:

```text
Candidate
   ↓
Select
```

without meaningful verification evidence, classify it as a finding.

Do not automatically redesign it outside D.3 scope.

---

# 29. EVOLUTION ↔ CERTIFICATION

Certification must remain downstream.

Evolution may consume certification evidence.

Evolution must never manufacture certification evidence.

Therefore:

```text
Evolution
   ↓
Candidate
   ↓
Verification
   ↓
Certification
```

is valid.

But:

```text
Evolution
   ↓
CERTIFIED
```

without verification/certification evidence is forbidden.

---

# 30. EVOLUTION ↔ RUNTIME FEEDBACK

Runtime observations must retain reverse lineage:

```text
RuntimeObservation
        ↓
ArtifactSet
        ↓
CompilerIR
        ↓
ArchitectureCandidate
        ↓
EvolutionRecord
        ↓
EvolutionOperation
        ↓
ParentCandidate
```

If current runtime telemetry cannot reconstruct this lineage, record the gap.

Do not fabricate runtime lineage.

---

# 31. HASHING AND IDENTITY

Investigate identity mechanisms for:

```text
ArchitectureCandidate
EvolutionOperation
EvolutionRecord
Lineage edge
Evaluation
Selection
```

Determine whether identities are:

```text
content-derived
semantic
random
database-generated
time-derived
```

Canonical identities must support reproducibility and provenance.

Do not introduce conflicting identity systems.

---

# 32. TEMPORAL SEMANTICS

Evolution records may contain:

```text
created_at
executed_at
evaluated_at
selected_at
```

Do not assume these are interchangeable.

If temporal semantics are ambiguous:

```text
DEFER
```

rather than inventing semantics.

---

# 33. VERSIONING

Determine how the repository handles:

```text
EvolutionOperation version
EIR version
ArchitectureCandidate version
operator version
engine version
contract version
```

A canonical evolution record must be able to identify the semantic contract under which it was created.

---

# 34. SECURITY

Treat evolution as a security boundary.

Investigate:

* untrusted evolution operations
* arbitrary mutation
* arbitrary code execution
* malicious candidate injection
* provenance spoofing
* lineage spoofing
* operator tampering
* seed manipulation
* constraint bypass

Do not introduce a new security framework during D.3.

Document required invariants and boundaries.

---

# 35. PERFORMANCE

Measure only where required to validate semantic behavior.

Do not optimize the Evolution Engine during R1-D.3.

If migration creates an obvious pathological performance regression:

```text
REPORT
```

rather than launching an optimization project.

---

# 36. CANONICAL OWNERSHIP RULE

At the end of D.3 there must be exactly one answer to:

```text
Where does EvolutionOperation live?
Where does EIR live?
Where does ArchitectureCandidate live?
Where does lineage live?
Where does evolution execution live?
Where does evaluation live?
Where does selection live?
Where does provenance live?
```

If any answer is:

```text
two competing places
```

the phase cannot receive PASS unless one is explicitly classified as:

```text
legacy
adapter
semantic donor
historical
```

---

# 37. GOVERNANCE BOUNDARY

The constitutional architecture layer may retain:

* governance rules
* invariants
* policies
* semantic specifications
* architectural views
* analysis
* validation models
* design rationale

It must not retain an independently executing Evolution Engine that competes with the canonical runtime.

---

# 38. REQUIRED RUNTIME GRAPH

Produce a final authoritative graph resembling:

```text
RequirementGraph
       │
       ▼
CanonicalISR
       │
       ▼
ArchitectureCandidate
       │
       ▼
CanonicalCompilerIR
       │
       ▼
CompilerBackend
       │
       ▼
ArtifactSet
       │
       ▼
VerificationResult
       │
       ▼
RuntimeObservation
       │
       ▼
Evaluation
       │
       ▼
Selection
       │
       ▼
EvolutionOperation
       │
       ▼
NewArchitectureCandidate
       │
       ▼
EvolutionRecord/EIR
       │
       └──────────────→ Lineage / Provenance
```

Any deviation must be explicitly justified.

---

# 39. GATE QUESTIONS

R1-D.3 may PASS only if all applicable questions are answered **YES**.

### Architecture

**G01** — Is there one canonical EvolutionOperation semantic authority?

**G02** — Is there one canonical EvolutionRecord/EIR authority?

**G03** — Is ArchitectureCandidate distinct from EvolutionRecord?

**G04** — Is Evolution distinct from CompilerIR?

**G05** — Is Evolution distinct from ISR?

### Semantics

**G06** — Are mutation semantics explicitly defined?

**G07** — Are crossover semantics explicitly defined?

**G08** — Are evaluation and selection semantically distinct?

**G09** — Is lineage explicitly reconstructable?

**G10** — Is provenance explicitly reconstructable?

### Integrity

**G11** — Are stochastic operations explicitly represented?

**G12** — Is reproducibility addressed?

**G13** — Are failure semantics fail-closed where required?

**G14** — Can invalid evolutionary operations be rejected?

**G15** — Can verification failure prevent unsupported evolutionary acceptance?

### Compatibility

**G16** — Are legacy evolution systems explicitly classified?

**G17** — Are adapters one-way?

**G18** — Are semantic losses explicitly documented?

**G19** — Are historical records immutable?

### Integration

**G20** — Does evolution consume Architecture Candidates rather than directly owning ISR semantics?

**G21** — Does evolution remain independent of compiler backend technology?

**G22** — Can runtime evidence feed evolutionary evaluation without bypassing provenance?

**G23** — Can lineage connect runtime evidence back to an evolutionary transition?

### Testing

**G24** — Are canonical contracts covered by tests?

**G25** — Are lineage semantics tested?

**G26** — Are mutation and crossover semantics tested?

**G27** — Are deterministic/stochastic semantics tested?

**G28** — Are failure semantics tested?

**G29** — Are provenance semantics tested?

### Architectural integrity

**G30** — Is there no second Evolution Engine remaining as a competing authority?

**G31** — Is there no category-specific evolutionary authority?

**G32** — Is there no bidirectional legacy/canonical adapter?

**G33** — Has no historical certification evidence been rewritten?

**G34** — Has no unrelated architecture been modified?

### Final

**G35** — Can the canonical evolutionary architecture be explained unambiguously from repository evidence?

All must be YES for:

```text
PASS
```

If any mandatory gate is NO:

```text
NOT_CERTIFIED
```

If evidence is insufficient:

```text
INDETERMINATE
```

Do not convert either into PASS.

---

# 40. VERDICT RULES

## PASS

Use only if:

* canonical evolution authority is established
* contracts are coherent
* lineage is explicit
* provenance is preserved
* semantic migration is complete within scope
* legacy boundaries are explicit
* tests pass
* no competing authority remains
* historical evidence is untouched

---

## PASS_WITH_DEFERRED_ITEMS

Allowed only when:

* the canonical architecture is coherent
* deferred items do not create competing semantic authority
* every deferred item is explicitly documented
* no mandatory gate is violated

---

## NOT_CERTIFIED

Use when:

* required implementation remains incomplete
* semantic contradictions remain
* canonical ownership is unresolved
* critical tests fail
* lineage/provenance is broken
* evolution bypasses required integrity boundaries

---

## INDETERMINATE

Use when:

* evidence is insufficient
* runtime behavior cannot be established
* repository state prevents reliable conclusions

Never infer PASS from absence of evidence.

---

# 41. COMMIT DISCIPLINE

Do not commit or push until the relevant deliverable set is complete and tests pass.

Use logically grouped commits.

Recommended grouping:

```text
D3-01 → D3-03
inventory + execution graph + semantic comparison

D3-04 → D3-07
canonical contracts + lineage model

D3-08 → D3-10
migration + consumers + legacy disposition

D3-11 → D3-13
integration + tests + gate
```

Do not amend prior commits.

Do not rewrite history.

Do not force-push.

Do not modify B3-v2 evidence.

If the governing workflow requires explicit authorization before Git mutation, STOP and request it rather than guessing.

---

# 42. REQUIRED FINAL REPORT

At completion report:

```text
R1-D.3 STATUS
=============
Verdict:
Baseline SHA:
Final SHA:
Origin synchronization:
Working tree:

Deliverables:
D3-01:
D3-02:
...
D3-13:

Canonical EvolutionOperation:
Canonical EvolutionRecord/EIR:
Canonical ArchitectureCandidate:
Canonical lineage:
Canonical provenance:

Legacy dispositions:

Tests:
Tier-A:
R1-C:
R1-D.1:
R1-D.2:
R1-D.3:
Combined:

Historical evidence:
B3-v2:
Certification ledgers:

Gate:
G01:
...
G35:

Deferred items:

Architectural findings:

Final recommendation:
```

---

# 43. HARD STOP CONDITIONS

STOP immediately if:

1. canonical ISR ownership changes unexpectedly
2. canonical Compiler IR ownership changes unexpectedly
3. historical certification evidence is modified
4. B3-v2 evidence is modified
5. a second canonical Evolution Engine is proposed
6. a second provenance authority is introduced
7. a bidirectional adapter is required
8. semantic loss cannot be characterized
9. required baseline cannot be established
10. unrelated subsystem changes become necessary
11. a new architectural conflict is discovered that cannot be resolved within D.3 scope
12. tests require weakening to pass
13. production evolution is proposed
14. autonomous self-modification becomes part of implementation scope

When stopping, produce a:

```text
R1-D.3 STOP REPORT
```

with:

* exact stopping point
* evidence
* affected contract
* architectural implication
* proposed resolution
* exact authorization required for continuation

---

# 44. WHAT SUCCESS LOOKS LIKE

R1-D.3 is successful when Tiannara has a coherent evolutionary substrate:

```text
                    ┌─────────────────────┐
                    │ Architecture Model  │
                    └──────────┬──────────┘
                               │
                               ▼
                    ┌─────────────────────┐
                    │ Architecture        │
                    │ Candidate           │
                    └──────────┬──────────┘
                               │
                       EvolutionOperation
                               │
             ┌─────────────────┼──────────────────┐
             ▼                 ▼                  ▼
          Mutation          Crossover          Transform
             │                 │                  │
             └─────────────────┼──────────────────┘
                               ▼
                    Constraint Validation
                               │
                               ▼
                         Compilation
                               │
                               ▼
                          ArtifactSet
                               │
                               ▼
                         Verification
                               │
                               ▼
                       Runtime Evidence
                               │
                               ▼
                          Evaluation
                               │
                               ▼
                           Selection
                               │
                               ▼
                    New Architecture
                               │
                               ▼
                       EvolutionRecord
                               │
                 ┌─────────────┴─────────────┐
                 ▼                           ▼
              Lineage                    Provenance
```

with:

```text
ONE ISR
ONE Architecture Model
ONE Compiler IR
ONE EvolutionOperation
ONE EIR
ONE Evolution authority
ONE provenance authority
ONE lineage model
```

and all other implementations explicitly classified as:

```text
DONOR
LEGACY
ADAPTER
PLUGIN
INFRASTRUCTURE
HISTORICAL
DEFERRED
RETIRED
```

---

# 45. FINAL PRINCIPLE

Do not optimize for the amount of code migrated.

Optimize for **semantic convergence**.

The objective is not:

```text
move evolution code
```

The objective is:

```text
establish one authoritative evolutionary semantics
```

that can eventually support:

```text
Architecture
    ↓
Evolution
    ↓
Competing Architectures
    ↓
Compilation
    ↓
Verification
    ↓
Runtime Evidence
    ↓
Evaluation
    ↓
Selection
    ↓
Evolution
```

This is the foundation required before Tiannara can safely evolve complete software systems autonomously.

**R1-D.3 must end with a gate and STOP.**

Do not automatically proceed to portfolio generation, full-stack evolution, autonomous self-engineering, or any subsequent phase.

Those require a separate architectural authorization after the D.1 + D.2 + D.3 substrate has been reconciled.

This is deliberately scoped so **D.3 completes the evolutionary half of the canonical compiler architecture without prematurely starting the portfolio/full-stack generation phase**.
