"""R2.10.9 — campaign readiness: corpus, harness, taxonomy, dry run.

The gate that applies the campaign's own discipline to itself: do not scale
an unverified boundary. R2.10.6–8 verified the compilation boundary; Phase
31 will stress it at thousands-project scale. This package proves the
campaign machinery — orchestration, measurement, and failure
classification — deterministic, budget-respecting, and ledger-faithful at
small scale before it runs at volume.

Two invariants govern the whole phase:

  * the harness is an EVIDENCE COLLECTOR, never an authority — it schedules
    and measures; it never touches the ISR or the verifier;
  * every generation outcome records its FULL provenance chain, so a Phase
    31 result is an auditable corpus of individually verifiable derivations,
    not an opaque success rate.
"""